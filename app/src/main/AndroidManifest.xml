<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlinedoctorappointmentservice">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnlineDoctorAppointmentService">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".DoctorLogin" />
        <activity android:name=".Feedback_Activity" />
        <activity android:name=".NueroSurgeon" />
        <activity android:name=".Cardiologist" />
        <activity android:name=".General_Physician" />
        <activity
            android:name=".DashBoard_Activity"
            android:label="@string/title_activity_dash_board_"
            android:theme="@style/Theme.OnlineDoctorAppointmentService.NoActionBar" />
        <activity android:name=".Dashboard_Activity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>